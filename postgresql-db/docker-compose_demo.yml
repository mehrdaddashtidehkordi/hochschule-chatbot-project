version: "3.9"

services:
  postgres:
    image: ****
    container_name: ****
    environment:
      POSTGRES_USER: ****
      POSTGRES_PASSWORD: ****
      POSTGRES_DB: dbname
    ports:
      - "****:****"
    volumes:
      - dbdata:/your directory address
      - ./init:/your directory

volumes:
  dbdata:
